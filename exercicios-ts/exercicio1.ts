// Declaração das variáveis com tipos
let produto: string = 'Mouse';
let preco: number = 59.90;
let disponivel: boolean = true;

// Exibir a mensagem no console
console.log(`produto: ${produto} - preço: R$${preco} - disponivel: ${disponivel ? 'Sim' : 'Não'}`);
